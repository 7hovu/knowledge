作为优化算法工程师，我在解决仓网规划的问题时，需要对调拨路径进行预处理，对片区内无生产可能性的sku，需要进行跨片区调拨。找到生产+调拨成本最低的路径，保留该路径，删去其他路径。输入数据如下：

需求表（self.workspace.warehouse_demand_plan）：['sku_code', 'stockorg_code', 'period', 'demand_qty']，主键为['sku_code', 'stockorg_code', 'period']

调拨路径（self.workspace.period_transfer_plan_base）：['send_stockorg_code', 'city_code', 'receive_stockorg_code','unit_trans_cost', 'sku_code', 'period', 'weight_ratio', 'transfer_qty', 'transfer_plan_id']，主键为：['send_stockorg_code',  'receive_stockorg_code', 'sku_code', 'period']

生产可能性及成本表（self.workspace.period_production_plan_base）：['sku_code', 'line_code', 'year', 'month','period', 'stockorg_code', 'unit_production_cost']，其中主键为['sku_code', 'line_code','period', 'stockorg_code']

片区映射关系（district）：['stockorg_code', 'district']
